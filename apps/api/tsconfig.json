{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "CommonJS",
    "moduleResolution": "Node10",
    // CommonJS cannot carry ESM syntax verbatim. See the note in CLAUDE.md.
    "verbatimModuleSyntax": false,
    // NestJS uses legacy parameter decorators, and emitDecoratorMetadata needs them
    // to record types for injection.
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "drizzle.config.ts", "vitest.config.ts"]
}
