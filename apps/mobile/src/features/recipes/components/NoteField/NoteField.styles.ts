import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  /** The button hugs the start of the line like the label it stands in for. */
  button: {
    alignItems: 'flex-start',
  },
});
